sudo: false
language: node_js
node_js:
  - 5.8.0
compiler:
  - gcc
  - clang
install:
env:
  - CXX=g++-4.8
addons:
  apt:
    sources:
    - ubuntu-toolchain-r-test
    packages:
    - gcc-4.8
    - g++-4.8
    - clang
